<h1>Devise Heirarchy Tester</h1>
<p>When user properly logged in, button below reads "ACTIVE"</p>
<% if user_signed_in? %>
<p><%= link_to "USER ACCESS", root_path, class: "btn btn-lg btn-primary" %></p>
  <% elsif admin_signed_in? %>
<p><%= link_to "ADMIN ACCESS", root_path, class: "btn btn-lg btn-success" %></p>
<% else %>
<p><%= link_to "NO ACCESS", root_path, class: "btn btn-lg btn-danger" %></p>
<% end %>
